import { Box, Button, Typography } from "@mui/material";
import React from "react";

const Finish = () => {
  return (
    <Box>
      <Typography>Go Home</Typography>
      <Button href="/app">Go</Button>
    </Box>
  );
};

export default Finish;
